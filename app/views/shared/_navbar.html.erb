<nav class="navbar">
  <div class="container">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'BugLog', root_path, class: "navbar-brand" %>

    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
            <%= content_tag :li, link_to('Sign out', destroy_user_session_path, method: :delete) %>
        <% else %>
            <%= content_tag :li, link_to('Sign in', new_user_session_path) %>
            <%= content_tag :li, link_to('New user?', new_user_registration_path) %>
        <% end %>
      </ul>

    </div>
  </div>

</nav>

